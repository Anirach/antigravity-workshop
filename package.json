{
    "name": "expense-tracker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "prisma generate",
        "vercel-build": "prisma generate && prisma migrate deploy && cd client && npm install && npm run build"
    },
    "dependencies": {
        "@prisma/client": "^5.22.0"
    },
    "devDependencies": {
        "prisma": "^5.22.0"
    }
}